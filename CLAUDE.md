# BIAV-SC — 忘却前夜 AI 增强插件

> 本文件为 Claude Code 自动加载入口。完整内容请读取 `BIAV-SC.md`。

请立即读取本仓库根目录的 **BIAV-SC.md**，那是平台无关的完整插件定义。

## 版本号规则（强制）

每次 `git push` 前**必须**更新版本号。违反此规则等同于忘记提交代码。

- 版本文件：`assets/data/VERSION.md`
- 格式：在当前版本号下追加本次变更的一行说明
- 如果本次变更是新功能/重大修改，递增次版本号（如 v0.9 → v0.10）
- 如果是文档/小修复，在当前版本下追加条目即可
- **不确定是否需要更新时，就更新。** 宁多勿少

## 沟通规则

- **始终使用中文**进行所有过程说明、状态报告和对话。代码注释和 commit message 可用英文。
