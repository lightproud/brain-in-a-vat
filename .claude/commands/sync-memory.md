Check all sub-project actual file states and sync shared memory files:

1. List files in each `projects/*/` directory
2. Compare actual state with `memory/project-status.md` — update if out of sync
3. Compare actual state with each `projects/*/CONTEXT.md` — update if out of sync
4. Check `assets/index.md` references point to existing files — fix broken references
5. Report all changes made
